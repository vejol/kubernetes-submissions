# Log Output App

This app is used with Ping-pong App.

Deploy both apps by running `kubectl apply -f manifests/` in the both project folders ([log_output](/log_output/) and [ping_pong](/ping_pong/))
