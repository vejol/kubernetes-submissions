# Project (Todo App)

Deploy app with `kubectl apply -f todo-app/manifests/ && kubectl apply -f todo-backend/manifests`

In k3d: Make sure you create persistent volume when launching app first time by running `kubectl apply -f .` in [persistent_volumes](/persistent_volumes/).
